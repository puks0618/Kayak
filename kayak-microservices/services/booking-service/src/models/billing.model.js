/**
 * Billing Model
 */

const BillingModel = {
  // id: UUID
  // booking_id: UUID
  // user_id: UUID
  // amount: DECIMAL
  // tax: DECIMAL
  // total: DECIMAL
  // payment_method: STRING
  // status: ENUM (paid, pending, refunded)
  // created_at: DATE
  // updated_at: DATE
};

module.exports = BillingModel;

